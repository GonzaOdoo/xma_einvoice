<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="account_move_inherit_einvoice" model="ir.ui.view">
            <field name="name">account.move.view.form.inherit</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <field name="payment_reference" position="before">
                    <field name="country_id"/>
                </field>
                <!-- <field name="invoice_date" position="replace">
                    <field name="invoice_date" invisible="1"/>
                    <field name="l10n_xma_date_post" string="Fecha de Factura"
                        nolabel="1" options="{'datepicker': {'warn_future': true}}" 
                        invisible="move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt')" 
                        readonly="state != 'draft'"
                    />
                </field> -->
                <xpath expr="//field[@name='invoice_line_ids']/list//field[@name='discount']"
                    position="after">
                    <field name="use_discount_rel" column_invisible="1"/>
                    <field name="l10n_xma_discount" optional="hide"
                        />
                </xpath>
            </field>
        </record>
    </data>
</odoo>
